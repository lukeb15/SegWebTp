@{
    ViewBag.Title = "Home Page";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
@if (Request.IsAuthenticated)
{    
     @Html.ActionLink("Create a Thread", "Create", "Thread", null, new { @class = "btn btn-info" })
}
    <table id="theads" class="table">
        <thead>
            <tr>
                <th></th>
                <th>Title</th>
                <th>Started by</th>
                <th>Created on</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in ViewBag.Threads)
            {
                <tr>
                    <td><img class="listIcon" src="@item.ImageLink" /></td>
                    <td>
                        @Html.ActionLink((string)@item.Title, "Details", "Thread", new { id = item.Id }, null)
                    </td>
                    <td>
                        @Html.ActionLink((string)@item.User.UserName, "Details", "User", new { id = @item.User.Id }, null)
                    </td>
                    <td>
                        @item.CreationDate
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>  